<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2015 Fabio Berta
  -->

<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <data>

        <import type="android.view.View"/>

        <variable
            name="viewModel"
            type="ch.giantific.qwittig.presentation.home.purchases.details.PurchaseDetailsTotalRowViewModel"
            />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

        <View
            android:id="@+id/divider"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_marginEnd="@dimen/key_line_1"
            android:layout_marginStart="@dimen/key_line_2"
            android:layout_marginTop="@dimen/small_space"
            android:background="?android:attr/listDivider"
            />

        <TextView
            style="@style/TextAppearance.AppCompat.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_below="@id/divider"
            android:layout_marginStart="@dimen/key_line_2"
            android:layout_marginTop="@dimen/normal_space"
            android:text="@string/purchase_total"
            />

        <TextView
            android:id="@+id/tv_total_value"
            style="@style/TextAppearance.AppCompat.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_below="@id/divider"
            android:layout_marginEnd="@dimen/key_line_1"
            android:layout_marginTop="@dimen/normal_space"
            android:text="@{viewModel.purchaseTotal}"
            tools:text="124.50 CHF"
            />

        <TextView
            android:id="@+id/tv_total_value_foreign"
            style="@style/TextAppearance.AppCompat.Subhead"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_below="@id/tv_total_value"
            android:layout_marginEnd="@dimen/key_line_1"
            android:layout_marginTop="2dp"
            android:text="@{viewModel.purchaseTotalForeign}"
            android:visibility="@{viewModel.purchaseTotalForeign.equals(viewModel.purchaseTotal) ? View.GONE : View.VISIBLE}"
            tools:text="1.34 GBP"
            tools:visibility="visible"
            />

    </RelativeLayout>
</layout>
