<?xml version="1.0" encoding="utf-8"?>

<!--
  ~ Copyright (c) 2015 Fabio Berta
  -->
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <data>

        <variable
            name="viewModel"
            type="ch.giantific.qwittig.presentation.finance.identities.IdentityRowViewModel"
            />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_avatar_with_text"
        android:orientation="horizontal"
        >

        <ImageView
            android:id="@+id/user_avatar"
            android:layout_width="@dimen/avatar_size"
            android:layout_height="@dimen/avatar_size"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="@dimen/key_line_2_minus_key_line_1_minus_avatar_size"
            android:layout_marginStart="@dimen/key_line_1"
            android:contentDescription="@string/user_avatar"
            app:avatar="@{viewModel.identityAvatar}"
            app:fallback="@{@drawable/ic_account_circle_black_40dp}"
            />

        <TextView
            style="@style/TextAppearance.AppCompat.Subhead.List"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:text="@{viewModel.identityName}"
            tools:text="Vera"
            />

        <TextView
            style="@style/TextAppearance.AppCompat.Subhead.List"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginEnd="@dimen/key_line_1"
            android:gravity="center_vertical"
            android:text="@{viewModel.identityBalance}"
            android:textColor="@{viewModel.balancePositive ? @color/green : @color/red}"
            tools:text="123.50"
            />

    </LinearLayout>

</layout>