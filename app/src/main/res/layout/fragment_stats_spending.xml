<!--
  ~ Copyright (c) 2015 Fabio Berta
  -->

<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <data>

        <import type="android.view.View"/>

        <variable
            name="viewModel"
            type="ch.giantific.qwittig.presentation.stats.spending.StatsSpendingViewModel"
            />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true"
        tools:context=".presentation.stats.spending.StatsSpendingFragment"
        >

        <ch.giantific.qwittig.presentation.stats.widgets.BarChart
            android:id="@+id/bc_spending"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="@dimen/small_space"
            android:layout_marginEnd="@dimen/key_line_1"
            android:layout_marginStart="@dimen/key_line_1"
            android:layout_marginTop="@dimen/small_space"
            android:visibility="@{viewModel.loading ? View.GONE : viewModel.dataEmpty ? View.GONE : View.VISIBLE}"
            app:barData="@{viewModel.barData}"
            app:dataEmpty="@{viewModel.dataEmpty}"
            />

        <include
            android:id="@+id/stats_empty_view"
            app:viewModel="@{viewModel}"
            layout="@layout/include_stats_empty_view"
            />

        <include
            android:id="@+id/stats_loading"
            app:viewModel="@{viewModel}"
            layout="@layout/include_progress"
            />

    </FrameLayout>

</layout>

